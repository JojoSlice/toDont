name: Build React Client on PR

on:
  pull_request:
    paths:
      - "Client/**"
      - "!Client/README.md"
    branches:
      - main
      - develop

permissions:
  contents: read
  pull-requests: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node-version: [20.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Count changed files and lines
        id: stats
        shell: bash
        run: |
          git fetch origin ${{ github.base_ref }}

          CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | grep '^Client/' | wc -l)
          echo "changed_files=$CHANGED_FILES" >> $GITHUB_OUTPUT

          CHANGED_LINES=$(git diff --numstat origin/${{ github.base_ref }}...HEAD | grep '^.*Client/' | awk '{sum+=$1+$2} END {print sum}')
          echo "changed_lines=${CHANGED_LINES:-0}" >> $GITHUB_OUTPUT

          TS_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -E '\.(ts|tsx)$' | wc -l)
          echo "ts_files=$TS_FILES" >> $GITHUB_OUTPUT

      - name: Show stats
        shell: bash
        run: |
          echo "📊 Statistik för React-projektet:"
          echo "================================"
          echo "Ändrade filer: ${{ steps.stats.outputs.changed_files }}"
          echo "Ändrade rader: ${{ steps.stats.outputs.changed_lines }}"
          echo "TypeScript/TSX filer: ${{ steps.stats.outputs.ts_files }}"
          echo "================================"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: Client/package-lock.json

      - name: Install dependencies
        working-directory: ./Client
        run: npm ci

      - name: Run ESLint
        working-directory: ./Client
        run: npm run lint

      - name: Run TypeScript type check
        working-directory: ./Client
        run: npx tsc -b --noEmit

      - name: Build project
        working-directory: ./Client
        run: npm run build

      - name: Build result
        if: success()
        shell: bash
        run: |
          echo "✅ Build lyckades på ${{ matrix.os }}!"
          echo "Ändrade ${{ steps.stats.outputs.changed_files }} filer med ${{ steps.stats.outputs.changed_lines }} rader"

      - name: Comment PR
        if: success() && matrix.os == 'ubuntu-latest'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 📊 React Build Statistik\n\n` +
                    `✅ Build lyckades på både Windows och Linux!\n\n` +
                    `### Ändringar i denna PR:\n` +
                    `- **Ändrade filer:** ${{ steps.stats.outputs.changed_files }}\n` +
                    `- **Ändrade rader:** ${{ steps.stats.outputs.changed_lines }}\n` +
                    `- **TypeScript/TSX filer:** ${{ steps.stats.outputs.ts_files }}\n\n` +
                    `✨ ESLint: Inga fel\n` +
                    `✨ TypeScript: Inga typfel\n` +
                    `✨ Build: Lyckades`
            })
